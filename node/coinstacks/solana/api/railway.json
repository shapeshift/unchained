{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "node/Dockerfile",
    "watchPatterns": ["node/coinstacks/solana/api/**", "node/coinstacks/common/**", "node/packages/**", "yarn.lock"]
  },
  "deploy": {
    "startCommand": "node dist/solana/api/src/app.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "multiRegionConfig": {
      "us-west2": {
        "numReplicas": 1
      }
    }
  }
}
