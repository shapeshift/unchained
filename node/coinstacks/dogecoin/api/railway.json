{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "Dockerfile",
    "buildArgs": {
      "DIR": "coinstacks/dogecoin/api",
      "PACKAGE": "@shapeshiftoss/dogecoin-api"
    },
    "watchPatterns": ["node/coinstacks/dogecoin/api/**", "node/coinstacks/common/**", "node/packages/**", "yarn.lock"]
  },
  "deploy": {
    "startCommand": "node dist/dogecoin/api/src/app.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "multiRegionConfig": {
      "us-west2": {
        "numReplicas": 1
      }
    }
  }
}
