{
  "name": "@shapeshiftoss/base-api",
  "version": "10.0.0",
  "license": "MIT",
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn generate && tsc",
    "clean": "rm -rf dist node_modules src/routes.ts src/swagger.json",
    "generate": "tsoa spec-and-routes"
  },
  "dependencies": {
    "@eth-optimism/contracts-ts": "^0.17.2",
    "@shapeshiftoss/blockbook": "^10.0.0",
    "@shapeshiftoss/common-api": "^10.0.0"
  }
}
