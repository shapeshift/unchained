{
  "build": {
    "builder": "dockerfile",
    "dockerfilePath": "Dockerfile",
    "buildArgs": {
      "DIR": "coinstacks/ethereum/api",
      "PACKAGE": "@shapeshiftoss/ethereum-api"
    },
    "watchPatterns": ["node/coinstacks/ethereum/api/**", "node/coinstacks/common/**", "node/packages/**", "yarn.lock"]
  },
  "deploy": {
    "startCommand": "node dist/ethereum/api/src/app.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "multiRegionConfig": {
      "us-west2": {
        "numReplicas": 1
      }
    }
  }
}
