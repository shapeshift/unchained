{
  "name": "@shapeshiftoss/unchained-proxy",
  "version": "10.0.0",
  "license": "MIT",
  "private": true,
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn generate && yarn run -T tsc",
    "clean": "rm -rf dist node_modules src/routes.ts src/swagger.json",
    "generate": "yarn run -T tsoa spec-and-routes"
  },
  "dependencies": {
    "@shapeshiftoss/common-api": "^10.0.0",
    "@shapeshiftoss/prometheus": "^10.0.0",
    "bottleneck": "^2.19.5",
    "elliptic-sdk": "^0.7.2",
    "lru-cache": "^10.2.0",
    "viem": "^2.33.2"
  }
}
