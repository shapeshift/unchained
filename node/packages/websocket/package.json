{
  "name": "@shapeshiftoss/websocket",
  "version": "10.0.0",
  "license": "MIT",
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn run -T tsc",
    "clean": "rm -rf dist node_modules",
    "dev": "yarn run -T nodemon -e ts --watch src -x yarn build",
    "watch": "yarn run -T nodemon -e ts --watch src -x yarn build"
  },
  "dependencies": {
    "@shapeshiftoss/prometheus": "^10.0.0",
    "ws": "^8.15.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10"
  }
}
