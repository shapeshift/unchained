{
  "name": "@shapeshiftoss/unchained-client",
  "version": "3.4.0",
  "license": "MIT",
  "main": "dist/index.js",
  "source": "src/index.ts",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "yarn generate && tsc",
    "clean": "rm -rf dist node_modules src/generated",
    "generate": "JAVA_OPTS='-Dlog.level=warn' openapi-generator-cli generate",
    "watch": "nodemon"
  },
  "dependencies": {
    "@shapeshiftoss/bitcoin-api": "^3.4.0",
    "@shapeshiftoss/common-ingester": "^3.4.0",
    "@shapeshiftoss/ethereum-api": "^3.4.0"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.4.4"
  }
}
